Запуск программы:
 1) start_server <port> <name_database> - Запуск сервера для работы с базой данных, указываем на какой порт будут поступать запросы в базу данных и имя файла базы данных
    При отсутсвии указанной базы данных, будет создана новая база данных.
 2) join_server <ip_address_server> <port> - Подключаемся к серверу базы данных для отправки запросов
 3) Команды:
            INSERT <имя таблицы (A или B)> <номер ID для записи в таблице(должен быть уникальным для каждой записи) от 1 до 9 символов> <строка с данными от 1 до 15 символов>
            TRANCATE <имя таблицы для удаления(A или B)>
            INTERSECTION (вывод пересечений таблиц A и B по ID )
            SYMMETRIC_DIFFERENCE (вывод симметричных пересечений таблиц A и B по ID)
            DEFRAGMINTATION_DB (дефрагминтация файла базы данных)